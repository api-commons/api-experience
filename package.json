{
  "name": "api-experience",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A DX/AX visual layer for any APIs.json — see each REST operation flow to its MCP tool and Agent Skill, with a free/paid coverage scorecard. Browser-only, no backend. An API Commons tool.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node tools/standalone.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "bundle": "node tools/bundle.mjs"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  }
}
